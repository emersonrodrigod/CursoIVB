/* Variável do tipo String(texto) */
var nomeDoProfessor = 'Emerson';

/*Variável do tipo inteiro (número inteiro) */
var numeroInteiro = 10;

/* Variável do tipo float (número com ,) */
var numeroFloat = 1.5;

/* Variável array */
var alunos = ['Allan', 'Marcelo', 'Filipe', 'Jonatas'];

/*variáveis tipo booleana (verdadeiro e falso)*/
var verdadeiro = true;
var falso = false;

